# Sandbox: local git worktree created by the API on first run
sandbox-repo/.git/

# SQLite run store (production / local)
backend/data/

# Python (backend)
backend/.venv/
**/__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
